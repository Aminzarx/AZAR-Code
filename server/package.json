{
  "name": "azar-auth-server",
  "version": "1.0.0",
  "private": true,
  "description": "AZAR CRM auth/referral backend (ADR-009 online surface). OTP delivery is disabled by design: any code is accepted at verify time. Owns only phone numbers, OTP-request state, referral relationships, and sessions — no business data (ADR-002/ADR-009).",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^11.9.1",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "pm2": "^5.4.3"
  }
}
