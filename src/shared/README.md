# shared/

Reusable, presentation-only building blocks with no feature-specific or
business logic: the future design-system component library (Phase 11,
`docs/ui/design-system.md`/`design-tokens.json`) and generic utility
functions used across more than one feature.
