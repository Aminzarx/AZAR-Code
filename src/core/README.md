# core/

The Local Application Core (per `ADR-001`'s consequences section): the
matching engine, contract/reminder logic, and backup orchestration land
here from Phase 6 onward as plain, platform-agnostic TypeScript with no
React or React Native import.

Intentionally empty in Phase 5 — no business logic exists yet.
